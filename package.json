{
  "name": "mcp-lite-wrappers",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight MCP wrappers that consolidate multiple tools into single-action tools",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
